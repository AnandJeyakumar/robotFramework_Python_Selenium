image: ocker pull marketsquare/robotframework-browser

stages:
  - test

my_test_job:
  stage: test
  script:
    - mkdir -p results
    - robot --outputdir results/ tests/
  artifacts:
    paths:
      - results/**
  environment:
    USERNAME: Ipatios Asmanidis  # Corrected indentation